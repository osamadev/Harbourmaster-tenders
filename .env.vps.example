# VPS self-hosted stack — copy to .env.docker on your server.
# Requires: docker compose --env-file .env.docker up --build -d

COMPOSE_PROFILES=local

GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
GEMINI_ANALYST_MODEL=gemini-2.5-pro
GEMINI_DRAFTER_MODEL=gemini-2.5-flash

PHOENIX_MODE=local
ELASTIC_MODE=local
RUNNING_IN_DOCKER=1

# Server-side (container-to-container) — keep these hostnames
PHOENIX_BASE_URL=http://phoenix:6006
PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:6006/v1/traces
ELASTIC_URL=http://elastic:9200
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=harbourmaster-elastic-local

# Browser links — replace with your VPS public IP or domain
PHOENIX_CONSOLE_URL=http://YOUR_VPS_HOST:6006

PHOENIX_PROJECT_NAME=harbourmaster
ELASTIC_ENABLED=true
ELASTIC_INDEX_PREFIX=harbourmaster

MCP_ENABLED=true
MCP_PHOENIX_ENABLED=true
MCP_ELASTIC_ENABLED=true

UI_PORT=8501
PHOENIX_PORT=6006
ELASTIC_PORT=9200
